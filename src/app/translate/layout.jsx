export const metadata = {
  title: "En → Fr Translator",
  description:
    "A from-scratch encoder-decoder transformer trained on WMT14 En-Fr, deployed on Modal.",
};

export default function TranslateLayout({ children }) {
  return children;
}
